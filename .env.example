# Railway Environment Variables Template
# Copy these to your Railway dashboard environment variables

# Required: Set your default login credentials
DEFAULT_USERNAME=your_username_here
DEFAULT_PASSWORD=your_password_here

# Optional: Streamlit configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# Optional: CrewAI configuration
CREWAI_STORAGE_DIR=./.crewai_storage
CHROMA_DB_IMPL=duckdb+parquet

# Note: Users will provide their GROQ API key through the login interface
# The API key is stored in session state and not in environment variables