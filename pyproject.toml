[project]
name = "yapyard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crewai",
    "streamlit",
    "chromadb==1.1.0",
    "pysqlite3-binary; sys_platform != 'win32'",
    "python-dotenv",
    "groq",
]
